name: qtaim_generator
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - rdkit
  - pymatgen=2023.3.23
  - lmdb
  - pip
  - pip:
      - bson
      - numpy==2.3.4

    # keep pip-only extras here if needed
